# Copy to apps/web/.dev.vars for `npm run preview:cf` (local OpenNext worker). Never commit .dev.vars.
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3200
